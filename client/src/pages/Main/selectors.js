export const selectRoomInfos = (state) => state.main.roomsInfos.infos;

export const selectDeviceInfos = (state) => state.main.deviceInfos;

export const selectCurrRoom = (state) => state.main.currRoom;

export const selectCurrDevice = (state) => state.main.currDevice;

export const selectDeviceDetail = (state) => state.main.DeviceDetail;
